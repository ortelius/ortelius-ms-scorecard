name: Build/Push Image and Release Charts

on:
  push:
    branches:
      - dev
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 2

      - name: Set env
        run: echo "IMAGE_TAG=v10.0.0.${{ github.run_number }}-g$(echo $GITHUB_SHA | cut -c 1-6)" >> $GITHUB_ENV

      - name: Configure Git
        run: |
          git config user.name "$GITHUB_ACTOR"
          git config user.email "$GITHUB_ACTOR@users.noreply.github.com"
          echo ${{ secrets.QUAY_USERID }} | base64
          echo ${{ secrets.QUAY_PASSWORD }} | base64

      - name: Login to Quay
        uses: docker/login-action@v2
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_USERID }}
          password: ${{ secrets.QUAY_PASSWORD }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v3
        with:
          push: true
          tags: quay.io/ortelius/ms-scorecard:${{ env.IMAGE_TAG }}

      - name: Install Helm
        uses: azure/setup-helm@v1
        with:
          version: v3.4.0
